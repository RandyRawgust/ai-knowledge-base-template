---
type: skill-doc
kind: slash-command
skill_name: commit-message-writer
description: Draft a conventional commit message from the staged git diff. Use before any commit when the user asks you to write the message or generate one. Triggers: commit message, write a commit msg, generate commit, what should I commit this as.
generated_by: 22 - Scripts/generate-skill-docs.ps1
last_generated: 05-19-26
tags: [skill, slash-command]
---

# /commit-message-writer

Draft a conventional commit message from the staged git diff. Use before any commit when the user asks you to write the message or generate one. Triggers: commit message, write a commit msg, generate commit, what should I commit this as.

## Where it lives

| Layer | Location |
|---|---|
| Runtime (what Claude invokes) | `.claude/skills/commit-message-writer/SKILL.md` |
| Vault doc (this file) | `03 - Skills & Rules/Skills/commit-message-writer.md` |
| Plugin manifest | `.claude-plugin/plugin.json` (auto-discovered) |

## Related

- Runtime: `.claude/skills/commit-message-writer/SKILL.md`

## Source of truth

The runtime SKILL.md is what Claude actually reads when invoking. This vault doc is a human-readable index entry — do not edit it by hand. To change the skill, edit `22 - Scripts/setup-claude-skills.ps1`, re-run it, then re-run `22 - Scripts/generate-skill-docs.ps1` to refresh this file.

> Auto-generated by `22 - Scripts/generate-skill-docs.ps1` on 05-19-26.